import { Activity, ArrowUpRight, Boxes, CircleAlert, Database, Network, Sparkles } from 'lucide-react'
import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { Link } from 'react-router-dom'
import { useUniverseModel } from '@/app/useUniverseModel'
import { Badge, Card, Progress } from '@/components/ui'
import { EntityDetailControl } from '@/components/EntityDetailControl'
import { analyzeNarrative } from '@/utils/narrative-analysis'
import { createSemanticIndex, getAllEntities } from '@/utils/semantic-index'

export function HomePage() {
  const { universe } = useUniverseModel()
  if (!universe) return null
  const entities = getAllEntities(universe)
  const index = createSemanticIndex(universe)
  const insights = analyzeNarrative(universe)
  const average = Math.round(entities.reduce((sum, entity) => sum + (entity.development ?? 0), 0) / Math.max(1, entities.length))
  const chartData = universe.modules.filter((module) => module.content.items?.length).map((module) => ({ name: module.title, development: Math.round((module.content.items ?? []).reduce((sum, item) => sum + (item.development ?? 0), 0) / Math.max(1, module.content.items?.length ?? 0)) }))
  const weak = insights.filter((insight) => insight.severity === 'high').length
  return <div className="dashboard"><section className="dashboard-hero"><div><p className="eyebrow">Universe control center</p><h1>{universe.metadata.title}<span>/{universe.metadata.subtitle ?? 'Lore Operating System'}</span></h1><p>{universe.metadata.description}</p></div><div className="version-card"><span>CANON BUILD</span><strong>v{universe.metadata.version}</strong><p>Updated {new Intl.DateTimeFormat('en', { month: 'short', day: 'numeric', year: 'numeric' }).format(new Date(universe.metadata.updated))}</p></div></section><section className="kpi-grid"><Card><div className="kpi-icon amber"><Database size={18} /></div><span>Canonical entities</span><strong>{entities.length}</strong><small>across {universe.modules.length} modules</small></Card><Card><div className="kpi-icon green"><Activity size={18} /></div><span>Development level</span><strong>{average}%</strong><Progress value={average} /></Card><Card><div className="kpi-icon blue"><Network size={18} /></div><span>Semantic links</span><strong>{[...index.references.values()].reduce((sum, refs) => sum + refs.length, 0)}</strong><small>cross-reference edges</small></Card><Card><div className="kpi-icon red"><CircleAlert size={18} /></div><span>Critical attention</span><strong>{weak}</strong><small>high-priority narrative signals</small></Card></section><section className="dashboard-grid"><Card className="chart-card"><div className="section-heading"><div><p className="eyebrow">Development map</p><h2>Module readiness</h2></div><Badge tone="amber">Live from source</Badge></div><div className="module-chart"><ResponsiveContainer width="100%" height="100%" minWidth={1} minHeight={1}><BarChart data={chartData} layout="vertical" margin={{ left: 0, right: 18 }}><XAxis type="number" domain={[0, 100]} hide /><YAxis dataKey="name" type="category" width={116} tick={{ fill: '#aaa', fontSize: 11 }} axisLine={false} tickLine={false} /><Tooltip cursor={{ fill: '#242424' }} contentStyle={{ background: '#181818', border: '1px solid #333', borderRadius: 10 }} /><Bar dataKey="development" fill="#f59e0b" radius={[0, 5, 5, 0]} barSize={12} /></BarChart></ResponsiveContainer></div></Card><Card className="attention-card"><div className="section-heading"><div><p className="eyebrow">Narrative intelligence</p><h2>Needs attention</h2></div><Link to="/insights"><ArrowUpRight size={18} /></Link></div><div className="attention-list">{insights.slice(0, 4).map((insight) => <div key={insight.id}><div className={`signal ${insight.severity}`}><Sparkles size={14} /></div><div><strong>{insight.title}</strong><p>{insight.detail}</p></div><span>{insight.score}</span></div>)}</div></Card><Card className="strength-card"><div className="section-heading"><div><p className="eyebrow">Creative north star</p><h2>Core strengths</h2></div><Boxes size={18} /></div><div>{entities.filter((entity) => (entity.development ?? 0) >= 80).slice(0, 4).map((entity) => <article key={entity.id}><div><strong>{entity.title}</strong><p>{entity.summary}</p></div><div className="strength-card-actions"><Badge tone="green">{entity.development}%</Badge><EntityDetailControl entity={entity} index={index} /></div></article>)}</div></Card></section></div>
}
